import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'maria-live-arena-test',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
